{
  "generated_at_utc": "2026-08-17T23:42:26.275095+00:00",
  "start_year": 2016,
  "end_year": 2025,
  "source": "Meteostat daily + daily_derived",
  "catalog_method": "GeoNames spatial grid, foreign cities within ~1000 km of French perimeter + priority cities",
  "cities_catalog": 699,
  "cities_with_data": 3,
  "rows": 36,
  "failures": {
    "Ajaccio": "FileExistsError: [Errno 17] File exists: '/home/runner/.meteostat/cache'",
    "Amiens": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Angers": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Angoulême": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Annecy": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Antibes": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Arcachon": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Arles": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Arras": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Aurillac": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Auxerre": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Avignon": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Bastia": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Bayonne": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Beauvais": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Belfort": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Bergerac": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Besançon": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Biot": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Blois": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Bordeaux": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Bourges": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Brest": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Briançon": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Brive-la-Gaillarde": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Caen": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Cahors": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Calais": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Carcassonne": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Carpentras": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Chalon-sur-Saône": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Chambéry": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Chartres": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Chaumont": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Cherbourg": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Châlons-en-Champagne": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Clermont-Ferrand": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Colmar": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Compiègne": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Dieppe": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Dijon": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Dunkirk": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Embrun": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Fougères": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Gap": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Grenoble": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Haguenau": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Hérouville-Saint-Clair": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "La Roche-sur-Yon": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "La Rochelle": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "La Teste-de-Buch": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Lannion": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Laval": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Le Creusot": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Le Havre": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Le Mans": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Le Puy-en-Velay": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Lille": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Limoges": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Lisieux": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Lorient": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Lyon": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Manosque": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Marne La Vallée": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Marseille": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Melun": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Mende": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Metz": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Mont-de-Marsan": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Montauban": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Montluçon": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Montpellier": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Montélimar": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Moulins": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Mulhouse": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Nantes": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Narbonne": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Nevers": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Nice": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Niort": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Nîmes": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Orléans": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Oyonnax": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Paris": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Pau": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Perpignan": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Poitiers": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Périgueux": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Quimper": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Reims": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Rennes": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Roanne": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Rodez": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Romans-sur-Isère": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Rouen": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Saint-Brieuc": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Saint-Dizier": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Saint-Leu": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Saint-Lô": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Saint-Malo": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Saint-Nazaire": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Saint-Quentin": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Saint-Étienne": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Saintes": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Sarreguemines": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Saumur": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Sedan": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Sens": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Soissons": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Strasbourg": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Tarbes": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Thionville": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Thonon-les-Bains": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Toulon": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Toulouse": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Tours": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Troyes": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Valbonne": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Valence": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Vannes": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Vichy": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Wattrelos": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Épernay": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Épinal": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Étampes": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Évreux": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Andorre-la-Vieille": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Graz": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Innsbruck": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Klagenfurt am Wörthersee": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Linz": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Salzburg": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Vienna": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Banja Luka": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Bihać": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Mostar": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Sarajevo": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Tuzla": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Antwerp": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Brussels": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Gent": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Liège": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Mons": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Ostend": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Basel": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Bern": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Geneva": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Genève": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Lausanne": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Lugano": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Sankt Gallen": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Sion": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Winterthur": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Zürich": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Brno": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Havířov": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Hradec Králové": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Kladno": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Liberec": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Olomouc": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Ostrava": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Pilsen": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Prague": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Zlín": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Ústí nad Labem": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "České Budějovice": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Aachen": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Aalen": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Arnsberg": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Augsburg": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Bamberg": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Bayreuth": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Berlin": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Bielefeld": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Bochum": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Bonn": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Braunschweig": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Bremen": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Bremerhaven": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Chemnitz": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Cottbus": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Dessau": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Detmold": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Dortmund": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Dresden": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Duisburg": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Düsseldorf": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Erfurt": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Essen": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Flensburg": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Frankfurt am Main": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Freiburg": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Fribourg-en-Brisgau": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Fulda": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Gera": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Göttingen": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Halle (Saale)": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Hamburg": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Hamburg-Mitte": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Hamburg-Nord": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Hanau am Main": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Hannover": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Hellersdorf": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Hildesheim": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Ingolstadt": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Jena": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Kaiserslautern": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Karlsruhe": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Kassel": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Kempten (Allgäu)": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Kiel": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Koblenz": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Konstanz": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Köln": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Landshut": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Leipzig": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Lübeck": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Lüneburg": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Magdeburg": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Mannheim": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Minden": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Munich": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Münster": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Neubrandenburg": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Neuwied": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Nuremberg": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Oldenburg": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Osnabrück": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Recklinghausen": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Regensburg": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Reutlingen": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Rheine": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Rosenheim": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Rostock": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Saarbrücken": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Salzgitter": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Schwerin": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Siegen": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Stuttgart": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Ulm": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Viersen": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Villingen-Schwenningen": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Wandsbek": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Wuppertal": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Würzburg": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Aabenraa": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Aalborg": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Aarhus": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Albertslund": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Allerød": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Avedøre": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Ballerup": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Birkerød": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Brøndbyvester": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Charlottenlund": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Copenhagen": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Copenhague": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Esbjerg": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Farum": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Fredericia": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Frederiksberg": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Frederikshavn": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Frederikssund": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Glostrup": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Greve": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Haderslev": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Hedensted": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Helsingør": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Herning": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Hillerød": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Hjørring": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Holbæk": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Holstebro": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Horsens": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Hvidovre": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Hørsholm": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Indre By": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Ishøj": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Kalundborg": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Klinteby Frihed": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Kolding": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Køge": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Lillerød": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Middelfart": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Nyborg": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Nykøbing Falster": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Næstved": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Nørresundby": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Odense": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Randers": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Ringsted": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Roskilde": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Rødovre": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Silkeborg": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Skive": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Slagelse": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Solrød Strand": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Stenløse": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Svendborg": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Sønderborg": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Taastrup": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Tilst": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Tønder": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Valby": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Vallensbæk": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Vanløse": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Vejle": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Viborg": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Viby": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Vordingborg": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Århus": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "A Coruña": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Albacete": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Alicante": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Almería": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Badajoz": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Barcelona": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Barcelone": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Bilbao": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Burgos": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Cadiz": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Campiña": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Cartagena": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Castelló de la Plana": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Ceuta": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Ciudad Real": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Collado-Villalba": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Cáceres": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Córdoba": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Donostia / San Sebastián": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Gandia": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Gasteiz / Vitoria": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Gijón": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Granada": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Guadalajara": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Huelva": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Jaén": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Jerez de la Frontera": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "León": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Lleida": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Logroño": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Lorca": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Lugo": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Madrid": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Marbella": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Mataró": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Melilla": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Mijas": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Murcia": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Málaga": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Ourense": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Palencia": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Palma": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Pamplona": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Ponferrada": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Salamanca": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Sant Martí": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Santander": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Santiago de Compostela": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Sevilla": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Talavera de la Reina": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Tarragona": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Toledo": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Valdemoro": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Valencia": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Valladolid": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Vigo": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Zamora": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Zaragoza": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Aberdeen": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Belfast": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Birkenhead": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Birmingham": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Bournemouth": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Bradford": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Brent": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Brighton": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Bristol": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Cambridge": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Cardiff": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Carlisle": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Chelmsford": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Coventry": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Dundee": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "East Kilbride": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Eastbourne": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Edinburgh": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Exeter": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Glasgow": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Gloucester": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Hereford": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Ipswich": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Islington": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Kingston upon Hull": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Leeds": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Leicester": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Lincoln": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Liverpool": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "London": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Londonderry County Borough": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Lowestoft": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Manchester": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Middlesbrough": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Milton Keynes": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Newcastle upon Tyne": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Newtownabbey": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Norwich": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Nottingham": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Oxford": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Peterborough": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Plymouth": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Preston": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Reading": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Saint Peters": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Scarborough": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Sheffield": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Southampton": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Southend-on-Sea": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Stoke-on-Trent": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Sunderland": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Swansea": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Swindon": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Telford": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Osijek": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Rijeka": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Split": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Zadar": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Zagreb": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Buda": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Budapest": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Budapest XVII. kerület": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Győr": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Kaposvár": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Kecskemét": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Miskolc": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Pest": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Pécs": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Sopron": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Szeged": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Szolnok": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Szombathely": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Székesfehérvár": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Zalaegerszeg": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Cork": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Dublin": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Galway": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Limerick": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "South Dublin": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Waterford": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Alessandria": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Ancona": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Andria": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Aoste": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Bari": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Bergamo": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Bologna": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Bolzano": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Brescia": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Brindisi": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Cagliari": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Caltanissetta": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Catania": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Catanzaro": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Como": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Cosenza": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Crotone": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Florence": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Foggia": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Gela": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Genoa": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Grosseto": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Imola": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "La Spezia": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Latina": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Livorno": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Marsala": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Matera": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Messina": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Mestre": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Milan": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Modena": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Naples": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Novara": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Olbia": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Padua": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Palermo": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Parma": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Perugia": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Pesaro": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Pescara": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Pisa": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Prato": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Ragusa": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Ravenna": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Rimini": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Rome": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Salerno": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Sanremo": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Sassari": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Savona": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Siracusa": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Taranto": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Terni": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Torre del Greco": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Trapani": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Trento": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Treviso": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Trieste": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Turin": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Udine": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Verona": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Luxembourg": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Amsterdam": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Apeldoorn": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Breda": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Eindhoven": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Enschede": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Groningen": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Haarlem": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Leeuwarden": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Maastricht": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Nijmegen": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Rotterdam": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "The Hague": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Utrecht": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Venlo": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Alta": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Arendal": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Asker": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Bergen": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Bodø": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Drammen": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Fredrikstad": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Gjøvik": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Grimstad": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Halden": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Hamar": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Harstad": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Haugesund": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Horten": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Kongsberg": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Kristiansand": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Kristiansund": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Larvik": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Levanger": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Lillehammer": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Lillestrøm": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Mo i Rana": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Molde": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Moss": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Narvik": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Nesttun": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Oslo": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Porsgrunn": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Sandefjord": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Sandnes": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Sarpsborg": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Skien": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Sola": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Stavanger": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Stjørdalshalsen": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Time": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Tromsdalen": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Tromsø": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Trondheim": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Tønsberg": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Ytrebygda": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Ålesund": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Bydgoszcz": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Częstochowa": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Gdańsk": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Gorzów Wielkopolski": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Grudziądz": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Głogów": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Jelenia Góra": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Kalisz": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Katowice": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Kielce": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Konin": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Koszalin": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Kraków": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Kędzierzyn-Koźle": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Legnica": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Nowy Sącz": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Opole": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Piotrków Trybunalski": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Piła": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Poznań": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Płock": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Stargard": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Szczecin": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Słupsk": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Tarnów": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Tomaszów Mazowiecki": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Toruń": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Wałbrzych": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Wrocław": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Włocławek": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Zielona Góra": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Łódź": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Braga": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Coimbra": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Faro": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Leiria": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Lisbon": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Porto": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Viseu": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Alingsås": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Björlanda": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Boo": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Borlänge": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Borås": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Bromma": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Danderyd": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Enköping": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Eskilstuna": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Falkenberg": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Falun": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Gothenburg": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Gustavsberg": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Gävle": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Göteborg": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Halmstad": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Haninge": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Helsingborg": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Huddinge": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Huskvarna": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Härnösand": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Jakobsberg": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Jönköping": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Kalmar": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Karlshamn": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Karlskoga": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Karlskrona": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Karlstad": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Katrineholm": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Kiruna": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Kristianstad": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Kungsholmen": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Kungälv": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Landskrona": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Lerum": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Lidingö": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Lidköping": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Linköping": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Luleå": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Lund": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Majorna": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Malmö": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Motala": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Märsta": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Mölndal": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Nacka": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Norrköping": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Nyköping": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Partille": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Piteå": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Sandviken": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Skellefteå": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Skövde": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Sollentuna": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Solna": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Stockholm": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Sundbyberg": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Sundsvall": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Södermalm": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Södertälje": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Trelleborg": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Trollhättan": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Tumba": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Täby": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Uddevalla": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Umeå": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Upplands Väsby": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Uppsala": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Vallentuna": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Varberg": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Vasastaden": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Visby": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Vänersborg": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Västervik": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Västerås": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Växjö": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Ängelholm": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Åkersberga": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Örebro": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Örnsköldsvik": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Östermalm": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Östersund": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Ljubljana": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Maribor": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Banská Bystrica": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Bratislava": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Košice": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Nitra": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed",
    "Žilina": "DatabaseError: Execution failed on sql '\n            SELECT\n                `id`,\n                `names`.`name` as `name`,\n                `country`,\n                `region`,\n                `latitude`,\n                `longitude`,\n                `elevation`,\n                `timezone`,\n                ROUND(\n                    (\n                        6371000 * acos(\n                            cos(radians(:lat)) * cos(radians(`latitude`)) * \n                            cos(radians(`longitude`) - radians(:lon)) + \n                            sin(radians(:lat)) * sin(radians(`latitude`))\n                        )\n                    ),\n                    1\n                ) AS `distance`\n            FROM\n                `stations`\n                INNER JOIN `names` ON `stations`.`id` = `names`.`station`\n                AND `names`.`language` = \"en\"\n            WHERE\n                `distance` <= :radius\n            ORDER BY\n                `distance`\n            LIMIT\n                :limit\n            ': database disk image is malformed"
  },
  "definition_sunny_day": "tsun > 300 minutes",
  "sun_spatial_fills": 12,
  "sun_fill_radius_km": 450.0
}